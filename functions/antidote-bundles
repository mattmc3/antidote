#!/usr/bin/env zsh

### Alias for `antidote bundle` to support legacy syntax.
###
### usage: antidote bundles [-h|--help] <bundle>...

local o_help
zparseopts -D -M -- h=o_help -help=h || return 1

if (( $#o_help )); then
  antidote-help bundles
  return
fi

antidote-bundle "$@"
