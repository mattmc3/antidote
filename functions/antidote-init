#!/usr/bin/env zsh

##? antidote-init - initialize the shell for dynamic bundles
##?
##? usage: antidote init
##?
##? This behavior exists mainly to support legacy antigen/antibody usage, but
##? static bundling is recommended for the best performance.

# This function changes how the `antidote` command works by sourcing the results of
# `antidote bundle` instead of just generating the Zsh script.

local script=(
  '#!/usr/bin/env zsh'
  'function antidote {'
  '  case "$1" in'
  '    bundle|bundles)'
  '      source <( _antidote_main $@ ) || _antidote_main $@'
  '      ;;'
  '    *)'
  '      _antidote_main $@'
  '      ;;'
  '  esac'
  '}'
)
printf "%s\n" "${script[@]}"
