#!/bin/zsh
# sanitize URL for safe use as a dir name
local bundle="$1"
local url=$(_antidote_tourl $bundle)
url=${url:gs/\@/-AT-}
url=${url:gs/\:/-COLON-}
url=${url:gs/\//-SLASH-}
echo $(antidote-home)/$url
