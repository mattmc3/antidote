# fake peterhurford/up.zsh

# this repo is interesting because the plugin file should be named up.zsh.plugin.zsh or
# the repo should just be called 'up', meaning we have to detect a non-standard plugin
# init file
