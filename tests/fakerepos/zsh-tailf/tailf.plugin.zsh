# fake rummik/zsh-tailf

# This repo is interesting because it's misnamed
# The plugin file should be named zsh-tailf.plugins.zsh or
# the repo should just be called 'tailf'
# Thus, there is a non-conforming Zsh plugin init file we need to detect
