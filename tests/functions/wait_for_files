#!/bin/zsh

#function wait_for_files {
  emulate -L zsh
  setopt local_options extended_glob
  local pattern=$1
  local -i expected=${2:-1} attempts=200
  local -a files

  while (( attempts-- )); do
    files=( ${~pattern}(N) )
    (( $#files >= expected )) && return 0
    sleep 0.01
  done
  return 1
#}
